export default function AdminUsersPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Users</h1>
      <p className="mt-2 text-sm text-text-muted">
        User search/moderation table — features/admin, following the same api/hooks/components
        pattern as every other feature slice.
      </p>
    </div>
  );
}
